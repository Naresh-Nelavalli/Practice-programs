#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int t;
    cin >> t;
     while(t--)
     {
        long long k = 0;
        long long n =0;
        long int digit =0;
        int count =0;
        cin >> n;
        cin >> k;
        for(int i=0;i<n;i++)
			{
        	cin >> digit;
        	if(digit <= 0)
        	{
        		count = count + 1;
			}
        	
			}
        
     }
    return 0;
}

